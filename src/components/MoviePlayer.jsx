import React from 'react'

/**
 * 
 * @param {id} Movie or Series ID 
 * @param {type} "movie" or "series"
 * @returns 
 */
const MoviePlayer = ({ id, type }) => {
  return (
    <div>MoviePlayer</div>
  )
};

export default MoviePlayer;